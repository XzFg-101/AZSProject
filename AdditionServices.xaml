<Window x:Class="AZSProject.AdditionServices"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AZSProject"
        mc:Ignorable="d"
        Title="AdditionServices" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions></Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="13*"></RowDefinition>
            <RowDefinition Height="194*"></RowDefinition>
            <RowDefinition Height="10*"></RowDefinition>
        </Grid.RowDefinitions>
        <ScrollViewer Grid.Row="1">
            <StackPanel>
                <Button Content="Apples"></Button>
                <Button Content="Apples"></Button>
                <Button Content="Apples"></Button>
                <Button Content="Apples"></Button>
                <Button Content="Apples"></Button>
                <Button Content="Apples"></Button>
                <Button Content="Apples"></Button>
            </StackPanel>
        </ScrollViewer>
        <TextBlock Text="Дополнительные услуги:"></TextBlock>
    </Grid>
</Window>
